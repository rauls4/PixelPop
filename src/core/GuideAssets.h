#pragma once
// The PixelPop User Guide, kept in flash (compressed with gzip) so the Home page can open it at /guide.
// The data is generated from "PixelPop User Guide.html" by tools/make_guide.py into GuideData.cpp:
// run it again (and upload the sketch) whenever the guide is changed.
#include <Arduino.h>

extern const uint8_t GUIDE_HTML_GZ[];
extern const size_t  GUIDE_HTML_GZ_LEN;
